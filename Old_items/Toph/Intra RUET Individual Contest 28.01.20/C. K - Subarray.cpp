#include<bits/stdc++.h>
using namespace std;

int main()
{
    long long n, i, k, j, sum;
    scanf("%lld", &n);
    long long arr[n], score[n];
    vector <long long> sa;

    for(i = 0; i < n; i++)
    {
        scanf("%lld", &arr[i]);
    }
    for(i = 0; i < n; i++)
    {

    }
}
