#include<bits/stdc++.h>
#include<vector>

using namespace std;

int main()
{
    int n, m, l, r, small, big, ina, inb, i;
    //vector <int> a, b;
    scanf("%d %d", &n, &m);
    int a[n], b[m];

    for(i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    for(i = 0; i < n; i++) {
        scanf("%d", &b[i]);
    }



}
