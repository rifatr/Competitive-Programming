#include<bits/stdc++.h>

using namespace std;

#define ll long long
#define lop(a, b) for(int i = a, i < b; i++)

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    double a, b, c, d, x;

    cin >> a >> b >> c >> d;



    return 0;
}

